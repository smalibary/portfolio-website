# Handoff — 2026-05-24

## Where things stand

Migrated the portfolio site (`website-personal/`, Jaspr static site on Cloudflare Pages, domain `smalibary.me`) from file-based content to a **Supabase backend**, then rebuilt the admin around it. `main` and `preview` are aligned at `9c3092f` and deployed to production.

Authoritative context lives in `DEPLOY.md` (env vars, Supabase architecture, admin auth, domain playbook, deploy hook) — **read it first**. Don't reconstruct from this doc.

## Working agreement (important — the user course-corrected on this)
- **One thing at a time.** Smallest fix that solves the actual problem. Do NOT bundle changes or propose delete-and-rebuild. The user got frustrated with big multi-component changes. (Saved to memory: `feedback_incremental_over_rewrite.md`.)
- **Commit everything together** when committing (`git add -A`, one commit/session). (Memory: `feedback_one_commit_per_session.md`.)
- Use `DESIGN.md` for any UI/CSS — 3-tier tokens, no raw hex/px in `.css` (inline styles on throwaway components are OK).
- Verify on the **preview** deploy before merging to `main` where practical; the user tests on production.

## What's done (see `git log`, commits `1f4c437`..`9c3092f`)
- **Supabase**: project `htpycoixfyqjatvtklmh` (Frankfurt). Schema `portfolio` (`posts`, `research_papers`, `profile`). Migrations in `supabase/migrations/`. Build reads Supabase via `website-personal/lib/data/supabase_client.dart`; data loaders are async, pre-loaded in `lib/main.server.dart`.
- **Admin auth**: Supabase Auth (email/password). `/api/admin/*` gated by `functions/api/admin/_middleware.js` → `_lib/auth.js` (`requireAdmin`: validates JWT via `/auth/v1/user`, checks `ADMIN_EMAILS`). Admin user: `salimmalibari@gmail.com` (temp pw was shared in chat — **user should rotate it**, see DEPLOY.md).
- **Admin UI**: list-first pattern. Blog (`lib/pages/admin/blog.dart`) and Research (`research.dart`) both = list of items → click → editor. Shared `components/admin/page_header.dart`. Publish triggers a Cloudflare deploy hook (`_lib/deploy.js`) → rebuild ~90s, with a countdown notice in the editor.
- **Build stamp**: `lib/components/build_stamp.dart` — self-contained floating badge (bottom-left, `branch@sha · time`) from CF Pages env. To remove later: delete that file + the `const BuildStamp()` line in `app.dart`. (Just fixed a `display:contents` bug that broke its `position:fixed`.)

## Next: Step 3 of the admin redesign (NOT started)
The user approved a unified-admin plan (one shell, two patterns: List + Editor). Steps 1 (shared page-header) and 2 (Research → list+editor) are done. **Step 3 remaining:**
1. Convert **Profile** (`lib/pages/admin/profile.dart`) to the editor pattern — it's a singleton (one row), so no list/back button, just the form + action bar, consistent with blog/research editors.
2. **Unify the topbar** — blog & research use a custom `header.topbar` (back button + section + current-title + chip); profile still uses the old `AdminTopbar` component (`components/admin/topbar.dart`). Make all three use one consistent topbar so there's no "jumping between pages."
3. Polish so all three admin pages feel identical.

## Known loose ends / cleanups (not urgent)
- Test/junk posts in Supabase (`ssijdof`, `thising-admin`, `hello`, etc. with garbage titles) — user may want to delete via admin and write a real article.
- Dead files from the old file-based system: `website-personal/content/blog/*`, `content/_data/*.yaml`, `tool/save_server.dart` — unused by the site now; safe to delete eventually.
- `tools/migrate_content_to_supabase.mjs` is the idempotent re-baseline script (reads files → upserts Supabase). Keep until the content files are removed.
- The build stamp is temporary — remove when no longer needed (one-file delete, see above).

## Verifying deploys
Each page shows `branch@sha · time` bottom-left. After a push/publish, hard-refresh (Ctrl+F5); if the SHA matches the latest commit, the page is current. Production poll pattern used this session: `curl -s https://smalibary.me/ | grep -oE 'current deploy">[^<]+'`.

## Suggested skills for next session
- `verify` — to drive the app/admin in a browser and confirm a change works.
- `code-review` — before merging the step-3 admin changes.
