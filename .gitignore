# =====================================================================
# Private working materials — never push to GitHub.
# =====================================================================
# Brand strategy, positioning, monetisation, and content brand context.
context/

# Drop zone for unsorted in-progress materials.
inbox/

# Throwaway design explorations (Track 1 / Track 2 mockup loops).
mockups/

# Private 100-item feature backlog.
ROADMAP.md

# Blog content is tracked so Cloudflare Pages CI can build it.
# Unpublished drafts can use a "status: draft" field in post.json.
# website-jaspr/content/blog/

# =====================================================================
# Build artifacts and tool caches.
# =====================================================================
.dart_tool/
build/
website-jaspr/.dart_tool/
website-jaspr/build/
website-jaspr/.packages
**/doc/api/
*.class
*.pyc
__pycache__/

# =====================================================================
# Editor / IDE / Claude tooling.
# =====================================================================
.claude/
.zed/
.idea/
.vscode/
.history/
.atom/
.buildlog/
*.iml
*.ipr
*.iws
migrate_working_dir/

# Local pi assistant state and prompt templates (may reference
# account-specific identifiers; keep local only).
.pi/
.wrangler/

# =====================================================================
# OS / shell debug junk.
# =====================================================================
*.stackdump
*.log
*.swp
.DS_Store
Thumbs.db
desktop.ini

# =====================================================================
# Secrets — defensive. None present today; this prevents accidental adds.
# =====================================================================
.env
.env.*
*.key
*.pem
*credentials*
*secret*
.superpowers/
.superpowers/
